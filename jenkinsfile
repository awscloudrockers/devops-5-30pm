pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Production-specific build steps
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to production...'
                // Production-specific deployment steps
            }
        }
    }
}
