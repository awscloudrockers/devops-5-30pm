pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building feature branch...'
                // Feature branch build steps
            }
        }
        stage('Test') {
            steps {
                echo 'Running feature tests...'
                // Feature branch test steps
            }
        }
    }
}
